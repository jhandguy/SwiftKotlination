package fr.jhandguy.swiftkotlination.model

import org.junit.Test

class ImageManagerUnitTest {

    lateinit var sut: ImageManager

    @Test
    fun `TODO`() {
        /*val image = File("27arizpolitics7-thumbLarge", File.Extension.JPG).data!!.readBytes()
        val networkManager = NetworkManagerMock(Result.Success(image))
        sut = ImageManager(networkManager)

        runBlocking {
            sut.image("") { result ->
                when(result) {
                    is Result.Success -> assertEquals(result.data, BitmapFactory.decodeByteArray(image, 0, image.size))
                    is Result.Failure -> fail(result.error.message)
                }
            }
        }*/
    }
}